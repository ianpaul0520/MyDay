const getNumber = 0;
const specifiedValue = 140;
let aDigit = Math.floor(Math.random() * Math.floor(20));
console.log(aDigit);
if (aDigit > specifiedValue) {
}

function tooHigh(n) {
  return n / 2;
}
function tooLow(n) {
  return n * 2;
}
